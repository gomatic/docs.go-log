{
  "baseURL": "https://gomatic.github.io/docs.go-log/",
  "title": "go-log",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-log — a CLI-agnostic log/slog configuration layer for Go."
  }
}
